pages:
  stage: deploy
  script:
    - chmod +x ./generate_readme.sh
    - ./generate_readme.sh
    - mkdir .public
    - cp -r * .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - main
